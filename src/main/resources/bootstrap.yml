jasypt:
    encryptor:
        algorithm: PBEWithMD5AndDES
        password: superkey

spring:
    cloud:
        config:
            server:
                bootstrap: false
            namespace: default
            enabled: true
            fail-fast: false 
            name: Cloud
            profile: dev, cloud, mysql
            label: master
        gcp:
            project-id: perceptive-day-252709
            credentials:
                location: classpath:credentials.json
            sql:
                enabled: true
                database-type: mysql
                database-name: marketplace
                instance-connection-name: ${PROJECT_ID:perceptive-day-252709}:us-central1:mysqlcontainer 